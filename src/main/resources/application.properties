spring.application.name=oms

# Server port
server.port=8112

# Application environment
env=local

# DynamoDB configuration
aws.dynamodb.endpoint=http://localhost:8111
aws.dynamodb.region=ap-northeast-1

# AWS credentials
aws.access.key.id=test
aws.secret.access.key=test

